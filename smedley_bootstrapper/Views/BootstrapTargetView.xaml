<UserControl x:Class="Smedley.Bootstrapper.Views.BootstrapTargetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Smedley.Bootstrapper.Views"
             xmlns:binders="clr-namespace:Smedley.Bootstrapper.Binders"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <local:PathTextBox DataContext="{Binding GameDirBinder}" HorizontalAlignment="Stretch" VerticalAlignment="Top" />
        <local:PathTextBox DataContext="{Binding KernelPathBinder}" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" />
    </Grid>
</UserControl>
